<local:WszystkieViewBase x:Class="Firma.Views.WszystkieFakturyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Firma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="numer" Binding="{Binding Numer}"/>
                <DataGridTextColumn Header="data wystawienia" Binding="{Binding DataWystawienia}"/>
                <DataGridTextColumn Header="Nazwa kontrachenta" Binding="{Binding KontrachentNazwa}"/>
                <DataGridTextColumn Header="NIP kontrachenta" Binding="{Binding KontrachentNIP}"/>
                <DataGridTextColumn Header="adres kontrachenta" Binding="{Binding KontrachentAdres}"/>
                <DataGridTextColumn Header="Termin platnosci" Binding="{Binding TerminPlatnosci}"/>
                <DataGridTextColumn Header="Sposob platnosci nazwa" Binding="{Binding SposobPlatnosciNazwa}"/>



            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
